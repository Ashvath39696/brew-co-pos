services:
  - type: web
    name: brew-co-pos
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node_modules/.bin/next start -p $PORT
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
